# Netlify — base directory trên dashboard: Visualization/frontend (hoặc frontend)
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
